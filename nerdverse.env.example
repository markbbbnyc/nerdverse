# nerdverse.env.example
# Copy this file to nerdverse.env and customize for your system.
# This file is safe to commit.
# The real nerdverse.env is git-ignored.

DB_USER=mark
DB_NAME=nerdverse2
DB_HOST=localhost

# If you use password auth instead of socket / .my.cnf, set:
# DB_PASS=yourpassword

# Optional: change these if your MariaDB setup differs
# DB_PORT=3306
